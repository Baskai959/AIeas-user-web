import { readFileSync } from 'node:fs';
import { resolve } from 'node:path';
import { describe, expect, it } from 'vitest';

const css = readFileSync(resolve(process.cwd(), 'src/styles.css'), 'utf8');

function rule(selector: string): string {
  const escapedSelector = selector.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
  const match = css.match(new RegExp(`${escapedSelector}\\s*\\{([\\s\\S]*?)\\}`, 'm'));
  return match?.[1] ?? '';
}

function rules(selector: string): string {
  const escapedSelector = selector.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
  return Array.from(css.matchAll(new RegExp(`${escapedSelector}\\s*\\{([\\s\\S]*?)\\}`, 'gm')))
    .map((match) => match[1])
    .join('\n');
}

function keyframes(name: string): string {
  const marker = `@keyframes ${name}`;
  const start = css.indexOf(marker);
  if (start < 0) return '';
  const next = css.indexOf('@keyframes', start + marker.length);
  return css.slice(start, next < 0 ? undefined : next);
}

describe('mobile layout CSS', () => {
  it('keeps formal live-room videos letterboxed while preserving immersive home previews', () => {
    expect(rule('.live-video')).toContain('object-fit: contain');
    expect(rule('.digital-human-video')).toContain('object-fit: contain');
    expect(rule('.discover-video')).toContain('object-fit: cover');
  });

  it('keeps looping video surfaces from receiving mobile tap events', () => {
    expect(rule('.live-video')).toContain('pointer-events: none');
    expect(rule('.digital-human-video')).toContain('pointer-events: none');
    expect(rule('.discover-video')).toContain('pointer-events: none');
    expect(rule('.room-thumb video')).toContain('pointer-events: none');
  });

  it('uses a stable mobile viewport height instead of dynamic compression for page shells', () => {
    expect(css).toContain('--app-stable-viewport-height: 100vh');
    expect(css).toContain('--app-stable-viewport-height: 100lvh');
    expect(rule('.tab-shell')).toContain('min-height: calc(var(--app-stable-viewport-height) - env(safe-area-inset-top))');
    expect(rule('.discover-page')).toContain('height: calc(var(--app-stable-viewport-height) - env(safe-area-inset-top) - var(--bottom-tab-height))');
    expect(rule('.orders-page')).toContain('height: calc(var(--app-stable-viewport-height) - env(safe-area-inset-top))');
  });

  it('uses one continuous bottom-tab background and highlights only active text/icons', () => {
    expect(css).toContain('--bottom-tab-height');
    expect(css).toContain('--app-shell-border-width');
    expect(rule('.bottom-tab-frame')).toContain('position: fixed');
    expect(rule('.bottom-tab-frame')).toContain('left: calc((100vw - min(100vw, var(--app-shell-max-width))) / 2 + var(--app-shell-border-width))');
    expect(rule('.bottom-tab-frame')).toContain('right: calc((100vw - min(100vw, var(--app-shell-max-width))) / 2 + var(--app-shell-border-width))');
    expect(rule('.bottom-tabs')).toContain('gap: 0');
    expect(rule('.bottom-tabs')).toContain('width: 100%');
    expect(rule('.bottom-tabs')).not.toContain('position: fixed');
    expect(rule('.bottom-tabs')).not.toContain('background: transparent');
    expect(rule('.bottom-tabs button')).toContain('background: transparent');
    expect(rule('.bottom-tabs button')).toContain('border-radius: 0');
    expect(rule('.bottom-tabs button.is-active')).not.toContain('background:');
    expect(rule('.tab-shell.is-home-tab .bottom-tabs')).toContain('background: rgba(5, 7, 13, 0.94)');
    expect(rule('.tab-shell.is-home-tab .bottom-tabs button')).toContain('color: rgba(255, 255, 255, 0.64)');
    expect(rule('.tab-shell.is-home-tab .bottom-tabs button.is-active')).toContain('color: #fff');
  });

  it('compresses the home preview lot strip and uses the transparent white live-entry button', () => {
    expect(rule('.discover-copy')).toContain('left: 16px');
    expect(rule('.discover-copy')).toContain('max-width: none');
    expect(rule('.discover-lot')).toContain('grid-template-columns: 35px minmax(0, 1fr) auto auto');
    expect(rule('.discover-lot')).toContain('justify-self: center');
    expect(rule('.discover-lot')).toContain('width: 100%');
    expect(rule('.discover-lot')).toContain('min-height: 44px');
    expect(rule('.discover-lot .visual-placeholder,\n.discover-lot img')).toContain('width: 35px');
    expect(rule('.discover-lot .visual-placeholder')).toContain('min-height: 0');
    expect(rule('.discover-lot .visual-placeholder small')).toContain('display: none');
    expect(rule('.discover-lot-status.is-running')).toContain('background: #ef4444');
    expect(rule('.discover-lot-status.is-upcoming')).toContain('border: 1px solid rgba(239, 68, 68, 0.34)');
    expect(rule('.discover-lot-schedule')).toContain('grid-column: 2 / -1');
    expect(rule('.discover-lot-schedule')).toContain('font-size: 11px');
    expect(rule('.discover-slide .discover-enter-button.adm-button')).toContain('background: rgba(255, 255, 255, 0.05)');
    expect(rule('.discover-slide .discover-enter-button.adm-button')).toContain('border: 1px solid rgba(255, 255, 255, 0.7)');
    expect(rule('.discover-slide .discover-enter-button.adm-button')).toContain('font-family: "Microsoft YaHei UI Light", "Microsoft YaHei", sans-serif');
    expect(rule('.discover-slide .discover-enter-button.adm-button')).toContain('font-weight: 300');
  });

  it('renders the home preview watcher count without a border', () => {
    expect(rules('.discover-watcher-count')).toContain('background: transparent');
    expect(rules('.discover-watcher-count')).not.toContain('border:');
  });

  it('keeps discover lot controls sticky and direction-revealable inside the lot feed', () => {
    expect(rule('.discover-lots-page')).toContain('height: calc(var(--app-stable-viewport-height) - env(safe-area-inset-top) - var(--bottom-tab-height))');
    expect(rule('.discover-lots-page')).toContain('overflow-y: auto');
    expect(rule('.discover-lots-toolbar')).toContain('position: sticky');
    expect(rule('.discover-lots-toolbar')).toContain('top: -14px');
    expect(rule('.discover-lots-toolbar')).toContain('transition: transform 180ms ease, opacity 180ms ease');
    expect(rule('.discover-lots-page.is-controls-hidden .discover-lots-toolbar')).toContain('pointer-events: none');
    expect(rule('.discover-lots-page.is-controls-hidden .discover-lots-toolbar')).toContain('transform: translateY(calc(-100% - 12px))');
  });

  it('uses the reference-style live-room header layout', () => {
    expect(css).toContain('--live-auction-action-width: clamp(112px, 30vw, 132px)');
    expect(rule('.live-header')).toContain('display: flex');
    expect(rule('.live-header')).toContain('padding: 6px 10px 0');
    expect(rule('.live-header')).toContain('z-index: 30');
    expect(rule('.live-back')).toContain('width: 28px');
    expect(rule('.live-back')).toContain('height: 28px');
    expect(rule('.live-back')).toContain('background: transparent');
    expect(rule('.live-shop')).toContain('grid-template-columns: 28px minmax(0, 1fr) auto');
    expect(rule('.live-shop')).toContain('width: fit-content');
    expect(rule('.live-shop')).toContain('max-width: min(66vw, 260px)');
    expect(rule('.live-shop')).toContain('background: rgba(31, 36, 49, 0.82)');
    expect(rule('.live-shop')).toContain('border-radius: 999px');
    expect(rule('.live-shop-logo')).toContain('width: 28px');
    expect(rule('.live-shop strong')).toContain('font-size: 12px');
    expect(rules('.live-shop span')).toContain('font-size: 9px');
    expect(rule('.live-follow-pill')).toContain('height: 24px');
    expect(rule('.live-follow-pill')).toContain('background: linear-gradient(135deg, #ff72a5 0%, #f44f92 100%)');
    expect(rule('.live-header-bidders')).toBe('');
    expect(rule('.live-header-bidder')).toBe('');
    expect(rule('.live-watcher-count')).toContain('background: transparent');
    expect(rule('.live-watcher-count')).toContain('opacity: 0.7');
    expect(rule('.live-watcher-count')).not.toContain('border:');
    expect(rule('.live-header-watchers')).toContain('min-width: 38px');
    expect(css).toContain('@media (max-width: 360px)');
    expect(rules('.live-header-bidders')).toBe('');
    expect(rule('.live-voice-permission')).toContain('z-index: 34');
    expect(rule('.live-voice-permission')).toContain('grid-template-columns: minmax(0, 1fr) auto');
    expect(rule('.live-voice-permission-button')).toContain('background: #fdb022');
    expect(rules('.live-voice-permission-button')).toContain('width: 100%');
  });

  it('keeps live-room bottom controls compact', () => {
    expect(css).toContain('--live-auction-action-width: clamp(112px, 30vw, 132px)');
    expect(rule('.live-page')).toContain('--live-comment-list-max-width: 280px');
    expect(rule('.live-comment-input-row')).toContain('grid-template-columns: 36px minmax(0, 1fr) 36px var(--live-auction-action-width)');
    expect(rule('.live-comment-input-row.is-collapsed')).toContain('display: flex');
    expect(rule('.live-comment-list')).toContain('width: min(var(--live-comment-list-max-width), calc(100% - var(--live-auction-action-width) - 22px))');
    expect(rule('.live-comment-list')).toContain('max-width: calc(100% - var(--live-auction-action-width) - 22px)');
    expect(rule('.live-comment-list')).toContain('max-height: min(30vh, 176px)');
    expect(rule('.live-comment-item')).toContain('display: flex');
    expect(rule('.live-comment-item')).toContain('align-items: flex-start');
    expect(rule('.live-comment-item span')).toContain('overflow-wrap: anywhere');
    expect(rule('.live-comment-item span')).toContain('white-space: normal');
    expect(rule('.comment-toggle-button,\n.comment-composer-send-button,\n.comment-list-button')).toContain('width: 36px');
    expect(rule('.comment-toggle-button.is-floating')).toContain('background: transparent');
    expect(rule('.comment-toggle-button.is-floating')).toContain('border: 0');
    expect(rule('.comment-input-trigger')).toContain('border-radius: 999px');
    expect(rule('.comment-like-button')).toContain('background: transparent');
    expect(rule('.comment-like-button')).toContain('border: 0');
    expect(rule('.comment-like-button')).toContain('box-shadow: none');
    expect(rule('.comment-like-button')).toContain('overflow: visible');
    expect(rule('.comment-like-button img')).toContain('filter: brightness(0) invert(1)');
    expect(rule('.comment-like-button.is-liked img')).toContain('hue-rotate(335deg)');
    expect(rule('.comment-like-burst')).toContain('inset: -10px');
    expect(rule('.comment-like-burst span')).toContain('width: 5px');
    expect(rule('.comment-like-burst span')).toContain('height: 5px');
    expect(rule('.comment-like-burst span')).toContain('--particle-color: #ff3158');
    expect(rule('.comment-like-burst span')).toContain('background: var(--particle-color)');
    expect(rule('.comment-like-burst span')).toContain('--particle-mid-distance');
    expect(rule('.comment-like-burst span')).toContain('animation: like-particle-burst');
    expect(rule('.comment-like-burst span')).toContain('cubic-bezier(0.06, 0.88, 0.18, 1)');
    expect(rule('.comment-like-burst span:nth-child(2)')).toContain('--particle-color: #ffd43b');
    expect(rule('.comment-like-burst span:nth-child(3)')).toContain('--particle-color: #3aa7ff');
    expect(rule('.comment-like-burst span:nth-child(15)')).toContain('--particle-angle: 336deg');
    expect(css).toContain('@keyframes like-particle-burst');
    expect(css).toContain('translateY(calc(-1 * var(--particle-mid-distance)))');
    expect(rule('.live-comment-panel.is-composing')).toContain('z-index: 45');
    expect(rule('.live-comment-compose-dismiss')).toContain('z-index: 1');
    expect(rule('.live-comment-compose-dismiss')).toContain('inset: 0 0 calc(54px + env(safe-area-inset-bottom))');
    expect(rule('.live-comment-composer')).toContain('grid-template-columns: minmax(0, 1fr) 56px');
    expect(rule('.live-comment-composer')).toContain('z-index: 2');
    expect(rule('.live-comment-composer textarea')).toContain('min-height: 36px');
    expect(rule('.live-comment-composer textarea')).toContain('max-height: 118px');
    expect(rule('.live-comment-composer textarea')).toContain('line-height: 20px');
    expect(rule('.comment-composer-send-button')).toContain('width: 56px');
    expect(rule('.comment-composer-send-button')).toContain('height: 36px');
    expect(rule('.comment-composer-send-button')).toContain('background: #f9a7bd');
    expect(rules('.comment-list-button')).toContain('width: var(--live-auction-action-width)');
    expect(rules('.comment-list-button')).toContain('background: linear-gradient(135deg, #ff7a18 0%, #ff2d55 100%)');
    expect(css).toContain('@media (max-height: 720px), (max-width: 360px)');
    expect(rule('.auction-float-card')).toContain('width: var(--live-auction-action-width)');
    expect(rule('.auction-float-card')).toContain('bottom: calc(66px + env(safe-area-inset-bottom))');
    expect(rules('.auction-float-card')).toContain('bottom: calc(56px + env(safe-area-inset-bottom))');
    expect(rule('.auction-float-card')).toContain('overflow: hidden');
    expect(rule('.auction-float-media')).toContain('position: relative');
    expect(rule('.auction-float-media')).toContain('width: calc(100% + 2px)');
    expect(rule('.auction-float-media')).toContain('aspect-ratio: 1 / 1');
    expect(rule('.auction-float-media')).toContain('margin: -1px -1px 0');
    expect(rule('.auction-float-media')).toContain('overflow: hidden');
    expect(rule('.auction-float-media')).toContain('border-radius: 10px 10px 10px 10px');
    expect(rule('.auction-float-media::after')).toContain('background: linear-gradient(180deg, rgba(0, 0, 0, 0.5) 0%, rgba(0, 0, 0, 0) 100%)');
    expect(rule('.auction-float-media::after')).toContain('height: max(28px, 15%)');
    expect(rule('.lot-result-card')).toContain('--lot-result-card-height: 128px');
    expect(rule('.lot-result-card')).toContain('--lot-result-thumb-size: var(--lot-result-card-height)');
    expect(rule('.lot-result-card')).toContain('height: var(--lot-result-card-height)');
    expect(rule('.lot-result-card')).toContain('max-height: var(--lot-result-card-height)');
    expect(rule('.lot-result-card')).toContain('padding: 0 12px 0 0');
    expect(rule('.lot-result-card')).toContain('overflow: hidden');
    expect(rule('.lot-result-card')).toContain('position: relative');
    expect(rule('.lot-result-card')).toContain('cursor: pointer');
    expect(rule('.lot-media-wrap')).toContain('width: var(--lot-result-thumb-size)');
    expect(rule('.lot-media-wrap')).toContain('height: var(--lot-result-thumb-size)');
    expect(rule('.lot-media-wrap')).toContain('aspect-ratio: 1 / 1');
    expect(rule('.lot-media-wrap')).toContain('position: relative');
    expect(rule('.lot-result-card .result-media')).toContain('width: 100%');
    expect(rule('.lot-result-card .result-media')).toContain('height: 100%');
    expect(rule('.lot-result-card .result-media')).toContain('aspect-ratio: 1 / 1');
    expect(rule('.lot-result-card .result-media')).toContain('border-radius: 8px');
    expect(rule('.lot-result-card h3')).toContain('margin: 0 0 5px');
    expect(rule('.lot-result-card h3')).toContain('font-size: 16px');
    expect(rule('.lot-status-line')).toContain('position: absolute');
    expect(rule('.lot-status-line')).toContain('bottom: 10px');
    expect(rule('.lot-result-card .status-badge')).toContain('background: rgba(0, 0, 0, 0.58)');
    expect(rule('.lot-info')).toContain('grid-row: 1 / 3');
    expect(rule('.lot-info')).toContain('padding: 14px 0 12px');
    expect(rule('.lot-merchant-link')).toContain('align-self: flex-start');
    expect(rule('.lot-merchant-link')).toContain('color: #1677ff');
    expect(rule('.lot-merchant-link')).toContain('font-size: 12px');
    expect(rule('.lot-merchant-link')).toContain('background: transparent');
    expect(rule('.lot-merchant-link')).toContain('border: 0');
    expect(rule('.lot-result-card .lot-price-line')).toContain('margin-top: auto');
    expect(rule('.lot-schedule-line')).toContain('width: fit-content');
    expect(rule('.lot-schedule-line')).toContain('font-variant-numeric: tabular-nums');
    expect(rule('.lot-schedule-line')).toContain('background: #fff7ed');
    expect(rule('.orders-page')).toContain('display: flex');
    expect(rule('.orders-page')).toContain('overflow: hidden');
    expect(rule('.orders-tab-row')).toContain('flex: 0 0 auto');
    expect(rule('.orders-page > .result-list')).toContain('overflow-y: auto');
    expect(rule('.orders-page > .result-list')).toContain('align-content: start');
    expect(rule('.orders-page > .result-list')).toContain('grid-auto-rows: max-content');
  });

  it('uses a merchant-only live card layout without eyebrow labels or oversized blank space', () => {
    expect(rule('.merchant-body')).toContain('display: grid');
    expect(rule('.merchant-body')).toContain('padding: 18px 14px 24px');
    expect(rule('.merchant-section-heading h2')).toContain('font-size: 23px');
    expect(rule('.merchant-section-heading h2')).not.toContain('margin: 0 0');
    expect(rule('.merchant-live-card')).toContain('grid-template-columns: 118px minmax(0, 1fr)');
    expect(rule('.merchant-live-card')).toContain('border-radius: 12px');
    expect(rule('.merchant-live-media')).toContain('min-height: 156px');
    expect(rule('.merchant-live-media')).toContain('background: #05070d');
    expect(rule('.merchant-live-body')).toContain('justify-content: space-between');
    expect(rule('.merchant-live-body')).toContain('padding: 14px 14px 14px 12px');
    expect(rule('.merchant-live-copy h3')).toContain('font-size: 22px');
    expect(rule('.merchant-live-copy h3')).toContain('-webkit-line-clamp: 2');
    expect(rule('.merchant-live-meta')).toContain('flex-wrap: wrap');
    expect(rule('.merchant-live-stat')).toContain('border-radius: 999px');
    expect(rule('.merchant-live-stat')).toContain('background: #f8fafc');
    expect(rule('.merchant-live-action.adm-button')).toContain('align-self: flex-start');
    expect(rule('.merchant-live-action.adm-button')).toContain('min-width: 112px');
    expect(css).toContain('@media (max-width: 360px)');
    expect(rules('.merchant-live-card')).toContain('grid-template-columns: 108px minmax(0, 1fr)');
  });

  it('docks the live ranking rail under the follow button with a low-alpha background', () => {
    expect(rule('.live-ranking-rail')).toContain('position: absolute');
    expect(rule('.live-ranking-rail')).toContain('right: 0');
    expect(rule('.live-ranking-rail')).toContain('top: 58px');
    expect(rule('.live-ranking-rail')).toContain('background: rgba(0, 0, 0, 0.2)');
    expect(rule('.live-ranking-rail')).toContain('--live-ranking-toggle-width: 20px');
    expect(rule('.live-ranking-rail')).toContain('--live-ranking-price-width: 64px');
    expect(rule('.live-ranking-toggle')).toContain('left: calc(-1 * var(--live-ranking-toggle-width))');
    expect(rule('.live-ranking-toggle')).toContain('height: 64px');
    expect(rule('.live-ranking-toggle')).toContain('writing-mode: horizontal-tb');
    expect(rule('.live-ranking-toggle')).toContain('border: 1px solid rgba(255, 255, 255, 0.46)');
    expect(rule('.live-ranking-toggle b')).toContain('writing-mode: vertical-rl');
    expect(rule('.live-ranking-rail.is-collapsed')).toContain('transform: translateX(100%)');
    expect(rule('.live-ranking-panel')).toContain('min-height: 0');
    expect(rule('.live-ranking-panel')).toContain('grid-template-rows: auto minmax(0, 1fr) auto');
    expect(rule('.live-ranking-panel')).toContain('align-content: start');
    expect(rule('.live-ranking-panel')).toContain('overflow: hidden');
    expect(rule('.live-ranking-board-viewport')).toContain('position: relative');
    expect(rule('.live-ranking-board-viewport')).toContain('min-height: 0');
    expect(rule('.live-ranking-board-viewport')).toContain('overflow: hidden');
    expect(rules('.live-ranking-rail')).toContain('width: clamp(146px, 42vw, 170px)');
    expect(rules('.live-ranking-panel')).toContain('max-height: min(calc(100dvh - 244px), 236px)');
    expect(rules('.live-ranking-board-viewport')).toContain('overflow-y: auto');
    expect(rule('.live-ranking-row')).toContain('grid-template-columns: 19px 22px minmax(0, 1fr) var(--live-ranking-price-width)');
    expect(rule('.live-ranking-title')).toContain('justify-content: center');
    expect(rule('.live-ranking-price')).toContain('color: #ff4d5f');
    expect(rule('.live-ranking-price')).toContain('justify-self: center');
    expect(rule('.live-ranking-price')).toContain('width: var(--live-ranking-price-width)');
    expect(rule('.live-ranking-price')).toContain('text-align: center');
    expect(rule('.live-ranking-price.is-leading-price')).toContain('font-size: 12px');
    expect(rule('.live-ranking-row.is-leading .live-ranking-name')).toContain('color: #ffd45f');
    expect(rule('.live-ranking-rank.is-plain')).toContain('background: transparent');
    expect(rule('.live-ranking-rank.is-plain')).not.toContain('border:');
    expect(rule('.live-ranking-divider')).toContain('margin: 0 2px');
    expect(rule('.live-ranking-current-row')).toContain('min-height: 28px');
    expect(rule('.live-ranking-current-row')).toContain('padding-top: 0');
    expect(rule('.live-ranking-row.is-moving-target')).toContain('animation: ranking-target-reveal var(--ranking-duration-ms');
    expect(css).toContain('.live-ranking-row.is-moving-target > .live-ranking-name');
    expect(css).toContain('.live-ranking-row.is-moving-target > .live-ranking-price');
    expect(css).toContain('opacity: 0');
    expect(rule('.live-ranking-exit-row')).toContain('animation: ranking-row-exit-divider var(--ranking-duration-ms');
    expect(rule('.live-ranking-exit-row')).not.toContain('grid-template-columns:');
    expect(rule('.live-ranking-exit-row')).not.toContain('font-size:');
    expect(rule('.live-ranking-exit-row')).not.toContain('filter:');
    expect(rule('.live-ranking-ghost')).toContain('top: var(--ranking-to-y');
    expect(rule('.live-ranking-ghost')).toContain('--ranking-pop-scale: 1.12');
    expect(rule('.live-ranking-ghost.is-self-bid')).toContain('--ranking-pop-scale: 1.3');
    expect(rule('.live-ranking-ghost.is-divider-to-first')).toContain('animation: ranking-divider-to-first var(--ranking-duration-ms');
    expect(rule('.live-ranking-ghost.is-current-row-to-first')).toContain('animation: ranking-current-row-to-first var(--ranking-duration-ms');
    expect(css).toContain('@keyframes ranking-price-update');
    expect(css).toContain('@keyframes ranking-top-slot-to-first');
    expect(css).toContain('@keyframes ranking-divider-to-first');
    expect(css).toContain('@keyframes ranking-current-row-to-first');
    expect(css).toContain('@keyframes ranking-row-exit-divider');
    expect(keyframes('ranking-row-exit-divider')).toContain('transform: translate3d(0, calc(var(--ranking-exit-to-y, 0px) - var(--ranking-exit-from-y, 0px)), 0)');
    expect(keyframes('ranking-row-exit-divider')).toContain('99%');
    expect(keyframes('ranking-row-exit-divider')).not.toContain('opacity: 0.72');
    expect(keyframes('ranking-row-exit-divider')).not.toContain('scale(');
  });

  it('animates live room sheets from the backdrop and bottom panel', () => {
    expect(rule('.sheet-backdrop')).toContain('z-index: 110');
    expect(rule('.sheet-backdrop')).toContain('--sheet-enter-duration-ms: 250ms');
    expect(rule('.sheet-backdrop')).toContain('--sheet-exit-duration-ms: 150ms');
    expect(rule('.sheet-backdrop')).toContain('--sheet-easing: linear');
    expect(rule('.sheet-backdrop')).not.toContain('animation: sheet-backdrop-in');
    expect(rule('.sheet-backdrop::before')).toContain('animation: sheet-backdrop-in var(--sheet-enter-duration-ms) var(--sheet-easing) both');
    expect(rule('.sheet-backdrop.is-closing::before')).toContain('animation: sheet-backdrop-out var(--sheet-exit-duration-ms) var(--sheet-easing) forwards');
    expect(rule('.sheet-layer')).toContain('z-index: 110');
    expect(rule('.sheet-layer')).toContain('background: transparent');
    expect(rule('.sheet-layer.is-closing .bottom-sheet')).toContain('animation: sheet-slide-down var(--sheet-exit-duration-ms) var(--sheet-easing) forwards');
    expect(rule('.bottom-sheet')).toContain('animation: sheet-slide-up var(--sheet-enter-duration-ms) var(--sheet-easing) both');
    expect(rule('.bottom-sheet')).toContain('will-change: transform');
    expect(rule('.sheet-backdrop.is-closing .bottom-sheet')).toContain('animation: sheet-slide-down var(--sheet-exit-duration-ms) var(--sheet-easing) forwards');
    expect(rule('.lot-list-sheet')).toContain('height: 70dvh');
    expect(rule('.lot-list-sheet')).toContain('max-height: 70dvh');
    expect(rule('.lot-list')).toContain('height: calc(100% - 52px)');
    expect(rule('.lot-list')).toContain('align-content: start');
    expect(rule('.lot-list')).toContain('max-height: none');
    expect(rule('.lot-list')).toContain('overflow-y: auto');
    expect(rule('.lot-row')).toContain('--lot-thumb-size: 116px');
    expect(rule('.lot-row')).toContain('--lot-row-height: var(--lot-thumb-size)');
    expect(rule('.lot-row')).toContain('height: var(--lot-row-height)');
    expect(rule('.lot-row')).toContain('grid-template-columns: var(--lot-thumb-size) minmax(0, 1fr) auto');
    expect(rule('.lot-row')).toContain('padding: 0 10px 0 0');
    expect(css).not.toContain('--lot-thumb-width');
    expect(rule('.lot-thumb-frame')).toContain('position: relative');
    expect(rule('.lot-thumb-frame')).toContain('align-self: start');
    expect(rule('.lot-thumb-frame')).toContain('width: var(--lot-thumb-size)');
    expect(rule('.lot-thumb-frame')).toContain('height: var(--lot-thumb-size)');
    expect(rule('.lot-thumb-frame')).toContain('aspect-ratio: 1 / 1');
    expect(rule('.lot-thumb-frame')).toContain('margin: -1px 0 -1px -1px');
    expect(rule('.lot-thumb-frame')).toContain('border-radius: 12px');
    expect(rule('.lot-row-meta')).toContain('display: flex');
    expect(rule('.lot-row-meta')).toContain('align-items: center');
    expect(rule('.lot-row-meta')).toContain('gap: 6px');
    expect(rule('.lot-row-meta .lot-schedule-line')).toContain('flex: 0 1 auto');
    expect(rule('.lot-row-meta .lot-schedule-line')).toContain('width: fit-content');
    expect(rule('.lot-row-meta .lot-schedule-line')).toContain('min-width: 0');
    expect(rule('.lot-row-meta .lot-schedule-line')).toContain('margin-top: 0');
    expect(css).not.toContain('.lot-thumb-frame::after');
    expect(rule('.lot-sequence')).toContain('position: absolute');
    expect(rule('.lot-sequence')).toContain('min-width: 18px');
    expect(rule('.lot-sequence')).toContain('height: 18px');
    expect(rule('.lot-sequence')).toContain('font-size: 9px');
    expect(rule('.lot-sequence')).toContain('background: rgba(15, 23, 42, 0.5)');
    expect(rule('.lot-sequence')).toContain('color: #fff');
    expect(rule('.lot-action-button.is-look')).toContain('background: #fff');
    expect(rule('.lot-action-button.is-disabled')).toContain('background: #e5e7eb');
    expect(rule('.detail-sheet')).toContain('height: 75dvh');
    expect(rule('.detail-sheet')).toContain('max-height: 75dvh');
    expect(rule('.detail-sheet')).toContain('display: flex');
    expect(rule('.detail-sheet')).toContain('overflow: hidden');
    expect(rule('.detail-sheet-header')).toContain('position: sticky');
    expect(rule('.detail-header-title')).toContain('display: flex');
    expect(rule('.detail-header-title')).toContain('align-items: center');
    expect(rule('.lot-gallery')).toContain('overflow: hidden');
    expect(rule('.lot-gallery-media-button')).toContain('touch-action: pan-y');
    expect(rule('.lot-gallery-track')).toContain('transition: transform 220ms ease');
    expect(rule('.lot-gallery-track.is-dragging,\n.lot-gallery-track.is-resetting')).toContain('transition: none');
    expect(rule('.lot-gallery-slide')).toContain('position: relative');
    expect(rule('.lot-gallery-slide')).toContain('overflow: hidden');
    expect(rule('.lot-gallery-slide img')).toContain('position: absolute');
    expect(rule('.lot-gallery-slide img')).toContain('top: 50%');
    expect(rule('.lot-gallery-slide img')).toContain('width: 100%');
    expect(rule('.lot-gallery-slide img')).toContain('height: auto');
    expect(rule('.lot-gallery-slide img')).toContain('transform: translateY(-50%)');
    expect(rule('.lot-gallery-slide img')).not.toContain('object-fit: cover');
    expect(rule('.lot-gallery-slide img')).toContain('object-position: center center');
    expect(rule('.image-viewer-backdrop')).toContain('position: fixed');
    expect(rule('.image-viewer-backdrop')).toContain('z-index: 1200');
    expect(rule('.image-viewer-image')).toContain('touch-action: none');
    expect(rule('.image-viewer-image img')).toContain('transform: translate3d(var(--viewer-offset-x, 0px), var(--viewer-offset-y, 0px), 0) scale(var(--viewer-scale, 1))');
    expect(rule('.image-viewer-reset')).toContain('left: 50%');
    expect(rule('.image-viewer-reset')).toContain('bottom: calc(112px + env(safe-area-inset-bottom))');
    expect(rule('.image-viewer-reset')).toContain('min-width: 96px');
    expect(rule('.image-viewer-reset')).toContain('height: 40px');
    expect(rule('.image-viewer-reset')).toContain('font-size: 16px');
    expect(rule('.image-viewer-reset')).toContain('background: rgba(31, 36, 49, 0.52)');
    expect(rule('.image-viewer-reset')).toContain('border-radius: 999px');
    expect(rule('.detail-scroll-body')).toContain('overflow-y: auto');
    expect(rule('.detail-sticky-actions')).toContain('position: sticky');
    expect(rule('.detail-rule-grid .metric')).toContain('justify-items: center');
    expect(rule('.detail-ranking-header')).toContain('justify-content: center');
    expect(rule('.detail-ranking-list')).toContain('max-height: calc(var(--detail-ranking-visible-rows) * 36px)');
    expect(rule('.detail-ranking-list')).toContain('transition: max-height 220ms ease, opacity 220ms ease');
    expect(rule('.detail-ranking-panel.is-expanded')).toContain('--detail-ranking-visible-rows: 8');
    expect(rule('.detail-ranking-actions')).toContain('justify-content: center');
    expect(rule('.detail-ranking-price.is-first')).toContain('color: #dc2626');
    expect(rule('.record-card.is-highlighted')).toContain('box-shadow: 0 0 0 2px rgba(255, 90, 102, 0.18)');
    expect(rule('.orders-page .record-card .result-media')).toContain('aspect-ratio: 1 / 1');
    expect(rule('.orders-page .record-card .result-media > img')).toContain('object-fit: cover');
    expect(rule('.orders-page .record-card .result-media > img')).toContain('object-position: center');
    expect(css).toContain('@keyframes sheet-slide-up');
    expect(keyframes('sheet-slide-up')).toContain('transform: translateY(100%)');
    expect(css).toContain('@keyframes sheet-slide-down');
    expect(keyframes('sheet-slide-down')).toContain('transform: translateY(100%)');
    expect(css).toContain('@media (prefers-reduced-motion: reduce)');
  });

  it('styles quick bid countdown segments and the filled leader badge', () => {
    expect(rule('.quick-bid-countdown-display')).toContain('display: inline-flex');
    expect(rule('.quick-bid-countdown-unit')).toContain('background: #ff8a00');
    expect(rule('.quick-bid-countdown-unit')).toContain('min-width: 34px');
    expect(rule('.quick-bid-countdown-unit.is-milliseconds')).toContain('min-width: 42px');
    expect(rule('.quick-bid-countdown-unit.is-milliseconds')).toContain('background: #fb7185');
    expect(rule('.quick-bid-countdown-separator')).toContain('color: #f97316');
    expect(rule('.quick-bid-countdown-separator.is-milliseconds')).toContain('color: #fb7185');
    expect(rule('.quick-bid-leader-badge')).toContain('background: #ffe3ec');
    expect(rule('.quick-bid-leader-badge')).toContain('border-radius: 999px');
    expect(rule('.quick-bid-leader-avatar')).toContain('background: linear-gradient(135deg, #7da4ff 0%, #6d5dfc 100%)');
  });

  it('shows and sinks auction floating cards into the goods button', () => {
    expect(rule('.auction-float-card.is-entering')).toContain('animation: auction-card-rise 380ms cubic-bezier(0.22, 1, 0.36, 1) forwards');
    expect(rule('.auction-float-card.is-ended .float-card-action')).toContain('background: #98a2b3');
    expect(rule('.auction-float-card.is-ended .float-card-action')).toContain('cursor: not-allowed');
    expect(rule('.auction-float-card.is-leaving')).toContain('animation: auction-card-sink 380ms cubic-bezier(0.22, 1, 0.36, 1) forwards');
    expect(rule('.auction-float-dismiss')).toContain('background: transparent');
    expect(rule('.auction-float-dismiss')).toContain('border: 0');
    expect(rule('.auction-float-dismiss')).toContain('top: 0');
    expect(rule('.auction-float-dismiss')).toContain('right: 0');
    expect(rule('.auction-float-dismiss')).toContain('color: #fff');
    expect(rule('.auction-float-dismiss')).toContain('width: 28px');
    expect(rule('.auction-float-dismiss svg')).toContain('width: 16px');
    expect(css).toContain('@keyframes auction-card-rise');
    expect(css).toContain('@keyframes auction-card-sink');
  });

  it('styles payment animation and receipt confirmation as mobile-first controls', () => {
    expect(rule('.payment-animation')).toContain('place-items: center');
    expect(rule('.payment-spinner')).toContain('animation: payment-spinner');
    expect(rule('.payment-flow')).toContain('animation: payment-flow');
    expect(rule('.payment-animation.is-paid .payment-check')).toContain('animation: payment-check-draw');
    expect(rule('.logout-choice-backdrop,\n.receipt-confirm-backdrop')).toContain('align-items: end');
    expect(rule('.logout-choice-panel,\n.receipt-confirm-panel')).toContain('border-radius: 18px 18px 0 0');
    expect(rule('.receipt-confirm-lot span')).toContain('text-overflow: ellipsis');
    expect(rule('.logout-choice-actions .adm-button,\n.receipt-confirm-actions .adm-button')).toContain('min-height: 44px');
  });

  it('renders auction atmosphere alerts above all live-room UI without blocking touches', () => {
    expect(rule('.live-auction-alert-layer')).toContain('position: fixed');
    expect(rule('.live-auction-alert-layer')).toContain('z-index: 1400');
    expect(rule('.live-auction-alert-layer')).toContain('pointer-events: none');
    expect(rule('.live-auction-alert')).toContain('animation: auction-alert-pop');
    expect(rule('.live-auction-alert.is-outbid')).toContain('animation-name: auction-alert-shake');
    expect(rule('.live-auction-alert.is-closed')).toContain('height: 100dvh');
    expect(rule('.live-auction-closed-heading')).toContain('text-shadow');
    expect(rule('.live-auction-closed-heading')).toContain('top: calc(50% - 198px)');
    expect(rule('.live-auction-alert.is-closed .live-auction-closed-card')).toContain('position: absolute');
    expect(rule('.live-auction-alert.is-closed .live-auction-closed-card')).toContain('top: 50%');
    expect(rule('.live-auction-alert.is-closed .live-auction-closed-card')).toContain('transform: translate(-50%, -50%)');
    expect(rule('.live-auction-closed-card em')).toContain('font-size: clamp(42px, 13vw, 56px)');
    expect(rule('.live-auction-alert.is-won')).toContain('animation: auction-success-enter');
    expect(rule('.live-auction-alert.is-won')).toContain('pointer-events: auto');
    expect(rule('.live-auction-success-heading')).toContain('text-shadow');
    expect(rule('.live-auction-alert.is-won .live-auction-alert-card')).toContain('top: 50%');
    expect(rule('.live-auction-alert.is-won .live-auction-alert-card')).toContain('transform: translate(-50%, -50%)');
    expect(rule('.live-auction-success-badge')).toContain('border-radius: 0 0 999px 999px');
    expect(rule('.live-auction-success-lot')).toContain('grid-template-columns');
    expect(rule('.live-auction-success-deposit')).toContain('grid-template-columns');
    expect(rule('.live-auction-success-pay')).toContain('min-height: 54px');
    expect(rule('.live-auction-success-close')).toContain('top: calc(50% + var(--auction-success-card-height) / 2 + 18px)');
    expect(rule('.live-auction-success-close')).toContain('border-radius: 50%');
    expect(css).toContain('@keyframes auction-alert-pop');
    expect(css).toContain('@keyframes auction-alert-shake');
    expect(css).toContain('@keyframes auction-success-enter');
    expect(css).toContain('@keyframes auction-alert-confetti-right');
  });

  it('styles the countdown pressure feedback through the global alert layer', () => {
    expect(css).not.toContain('.live-countdown-pressure');
    expect(rule('.live-countdown-ambient')).toContain('z-index: 4');
    expect(rule('.live-countdown-ambient')).toContain('pointer-events: none');
    expect(rule('.live-countdown-ambient')).toContain('--countdown-ambient-band-width: clamp(7.5px, 2.1vw, 12px)');
    expect(rule('.live-countdown-ambient')).toContain('--countdown-ambient-normal-width: clamp(21px, 6vw, 30px)');
    expect(rule('.live-countdown-ambient')).toContain('--countdown-ambient-pulse-width: clamp(42px, 12vw, 60px)');
    expect(rule('.live-countdown-ambient-band')).toContain('width: var(--countdown-ambient-band-width)');
    expect(rule('.live-countdown-ambient-band')).toContain('height: var(--countdown-ambient-progress, 0%)');
    expect(rule('.live-countdown-ambient-band')).toContain('animation: countdown-ambient-breathe');
    expect(rule('.live-countdown-ambient-bloom')).toContain('width: var(--countdown-ambient-normal-width)');
    expect(rule('.live-countdown-ambient-bloom')).toContain('top: calc(100% - var(--countdown-ambient-progress, 0%))');
    expect(rule('.live-countdown-ambient-particles')).toContain('width: var(--countdown-ambient-normal-width)');
    expect(rule('.live-countdown-ambient-particles')).toContain('height: var(--countdown-ambient-progress, 0%)');
    expect(rule('.live-countdown-ambient-particle')).toContain('animation: countdown-ambient-particle-float');
    expect(rule('.live-countdown-ambient-pulse')).toContain('animation: countdown-ambient-bid-pulse');
    expect(rule('.live-countdown-ambient-pulse::before,\n.live-countdown-ambient-pulse::after')).toContain('width: var(--countdown-ambient-pulse-width)');
    expect(rule('.live-countdown-ambient-pulse-sparks')).toContain('width: var(--countdown-ambient-pulse-width)');
    expect(rule('.live-countdown-ambient-pulse-sparks')).toContain('overflow: hidden');
    expect(rule('.live-countdown-ambient-pulse-spark')).toContain('animation: countdown-ambient-pulse-spark');
    expect(rule('.live-countdown-ambient-pulse-sparks.is-right .live-countdown-ambient-pulse-spark')).toContain('right: var(--countdown-ambient-band-width)');
    expect(rule('.live-countdown-ambient.is-end-leaving .live-countdown-ambient-band.is-left,\n.live-countdown-ambient.is-end-leaving .live-countdown-ambient-particles.is-left')).toContain('transform: translateX(-140%)');
    expect(rule('.live-countdown-ambient.is-end-leaving .live-countdown-ambient-band.is-right')).toContain('transform: translateX(140%) scaleX(-1)');
    expect(rule('.live-countdown-ambient.is-end-leaving .live-countdown-ambient-bloom.is-left')).toContain('transform: translate(-140%, -50%)');
    expect(rule('.live-countdown-ambient.is-other')).toContain('--countdown-ambient-rgb: 255, 49, 88');
    expect(rule('.live-countdown-ambient.is-self')).toContain('--countdown-ambient-rgb: 255, 190, 64');
    expect(rule('.sheet-backdrop')).toContain('z-index: 110');
    expect(rule('.sheet-layer')).toContain('z-index: 110');
    expect(rule('.live-auction-alert.is-countdown')).toContain('animation-duration: 1400ms');
    expect(rule('.live-auction-alert.is-countdown .live-auction-alert-card')).toContain('background: linear-gradient');
    expect(rule('.live-auction-alert.is-countdown .live-auction-alert-value')).toContain('font-size: clamp(58px, 18vw, 86px)');
    expect(rule('.live-auction-alert.is-countdown.is-warning .live-auction-alert-card')).toContain('animation: countdown-alert-pulse');
    expect(rule('.live-auction-alert.is-countdown.is-critical')).toContain('animation-name: auction-alert-shake');
    expect(rule('.live-auction-alert.is-countdown.is-critical .live-auction-alert-card')).toContain('animation: countdown-alert-critical');
    expect(rule('.live-auction-alert.is-countdown.is-extended .live-auction-alert-card')).toContain('animation: countdown-alert-extended');
    expect(rule('.auction-float-countdown.is-warning')).toContain('color: #ffb020');
    expect(rule('.auction-float-countdown.is-critical')).toContain('animation: float-countdown-critical');
    expect(rule('.quick-bid-countdown.is-warning .quick-bid-countdown-unit')).toContain('animation: quick-countdown-warning');
    expect(rule('.quick-bid-countdown.is-critical .quick-bid-countdown-unit')).toContain('animation: quick-countdown-critical');
    expect(css).toContain('@keyframes countdown-ambient-breathe');
    expect(css).toContain('@keyframes countdown-ambient-particle-float');
    expect(css).toContain('@keyframes countdown-ambient-bid-pulse');
    expect(css).toContain('@keyframes countdown-ambient-pulse-spark');
    expect(css).toContain('@keyframes countdown-alert-pulse');
    expect(css).toContain('@keyframes countdown-alert-critical');
    expect(css).toContain('@keyframes countdown-alert-extended');
    expect(css).toContain('@keyframes float-countdown-critical');
    expect(css).toContain('@keyframes quick-countdown-warning');
    expect(css).toContain('@keyframes quick-countdown-critical');
  });
});
