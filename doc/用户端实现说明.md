# 用户端实现说明

## 当前范围

本项目实现直播竞拍全栈系统的移动端 H5 用户端。当前版本以“可独立演示完整直播竞拍体验”为目标，默认使用本地 Demo 数据和前端 Mock WebSocket。

已实现链路：

- 登录
- 首页直播推荐流，支持移动端上下滑动、首尾循环切换和 PC 拖拽/滚轮模拟
- 发现页拍品列表
- 全屏直播间与固定视频模拟；从首页直播预览进入时，同源视频会承接预览播放进度
- REST 配置的直播间视频源：实景视频或数字人视频
- 数字人待机/说话双视频层，用户端不暴露播报控制
- 直播间评论展示、评论输入、评论开关和评论 WebSocket 事件
- 右下角竞拍清单入口
- 直播间商品列表半屏底部抽屉，内部可滚动，当前竞拍中的拍品置顶显示
- 拍品详情半屏弹层
- 报名与保证金模拟冻结
- 手动出价、步进加价、本地基础校验
- 当前最高价、起拍价、落槌价按状态展示
- 实时在线人数、排行榜、出价 ACK、领先、延时、成交、场次结束事件
- 成交结果、模拟支付、历史订单
- “我的”页关注/足迹入口、我的订单入口、关注列表、足迹列表和订单列表页

## 技术选型

- React 18 + TypeScript + Vite
- Ant Design Mobile 作为移动端 UI 基础组件
- Zustand 管理会话状态
- TanStack Query 管理 REST 数据请求
- React Router 管理页面路由、深链和登录守卫
- 原生 WebSocket 抽象管理实时通信

旧版用户端需求文档曾同时提到 `vant / antd-mobile / Tailwind` 任选。由于当前项目采用 React，Vant 属于 Vue 生态，本实现继续使用 Ant Design Mobile，不引入 Vant。

## 接口来源

当前以后端新推送文档为准：

- `doc/openapi/default.openapi.json`
- `doc/openapi/live-room-websocket.openapi.json`
- `doc/拍卖客户端实时交互方案.md`

旧 `doc/Apifox接口.html`、`doc/API文档.docx`、`doc/接口对齐备忘.md` 已被新文档和 `doc/接口对齐文档.md` 取代。

默认 API 客户端使用本地 Demo 数据，不再默认访问 Apifox REST Mock。真实联调时显式设置：

```env
VITE_API_MODE=remote
VITE_API_BASE_URL=http://127.0.0.1:4523/m1/8317345-8081123-default
```

## 实时通信

实时通信按原生 WebSocket envelope：

```ts
{
  type: string
  requestId?: string
  seq?: number
  ack?: boolean
  liveSessionId?: number
  payload: Record<string, unknown>
}
```

当前默认 `MockRealtimeClient`，覆盖：

- `room.subscribed`
- `room.online`
- `heartbeat.ack`
- `bid.ack`
- `bid.accepted`
- `bid.rejected`
- `ranking.updated`
- `room.snapshot_required`
- `timer.extended`
- `auction.closed`
- `live_session.ended`
- `chat.ack`
- `chat.message`
- `chat.error`

客户端维护 `lastSeq`，重复或乱序事件不应用。真实 WS 模式连接：

```text
{VITE_WS_URL}/ws/live-rooms/{roomId}?lastSeq={lastSeq}
```

收到 `room.snapshot_required` 时，用户端应以 REST 快照接口刷新状态；当前实现的恢复来源为 `GET /api/v1/auctions/{id}/state`。

评论也走同一条直播间 WebSocket：

```ts
{ type: 'chat.send', requestId, payload: { roomId, content, clientMessageId } }
{ type: 'chat.ack', requestId, ack: true, payload: { roomId, clientMessageId, messageId, createdAt } }
{ type: 'chat.message', seq, payload: { id, roomId, userId, nickname, avatarUrl?, content, createdAt, clientMessageId? } }
{ type: 'chat.error', requestId, payload: { roomId, clientMessageId?, message } }
```

## 数字人视频源

直播间视频源由后端/商家配置决定，用户端不提供实景/数字人手动切换。`LiveRoom.videoSource` 为 `recorded` 时使用 `videoUrl`；为 `digitalHuman` 时使用 `digitalHuman` 配置。前端按 `h5_node_client.mjs` 和 `new_doc/H5用户端移植指南.md` 的浏览器端视频组织方式实现数字人画面，但用户端不提供播报文本、启用声音、发送播报、停止、重连等控制元素。数字人播报应由商家配置或服务端驱动，不由观看端用户触发。从首页直播预览进入正式直播间时，前端会通过 React Router location state 传递预览视频的 `currentTime`；只有直播间和视频源 URL 均匹配时才在正式直播间应用该进度。

- 待机视频和说话视频均为静音 mp4，前端叠加两层视频；接收 `audio_start` 后显示说话视频，播放队列清空后回到待机视频。
- `digitalHuman.ttsWsUrl` 仍可作为后续服务端驱动音频流的配置字段，但当前用户端直播间不主动显示或触发 TTS 控制流程。
- 仓库当前内置数字人视频素材：`public/media/AI_Presenter_Silent.mp4` 和 `public/media/AI_Presenter_Speaking.mp4`。

## 本轮重构

- 将首页从拍品列表改为直播间列表。
- 将 `/auction/:id` 替换为 `/live/:roomId`。
- 引入 `LiveRoom`、`LiveRoomLot`、`LiveRoomStats` 模型。
- 移除旧 `/api/v1/auctions`、`/api/v1/auctions/{id}`、`/api/v1/auctions/mine` 依赖。
- 将 `直播间视频.mp4` 迁移为 `public/media/live-room-demo.mp4`。
- 基于参考 UI 重做直播间、商品列表、拍品详情、出价弹层。
- 将 `new_doc` 文档整合到 `doc` 下，并删除旧接口文档。

## 本地化

所有 UI 显示字符串集中维护在 `src/i18n/messages.ts`，默认 `zh-CN`，同步维护 `en-US`。组件不得直接硬编码展示文案。

## 2026-05-30 首页、搜索与商家页重构

本轮曾基于 `参考UI/首页UI.jpg`、`参考UI/搜索页UI.jpg`、`参考UI/商家页UI.jpg` 重新设计用户端移动端信息架构；当前主导航和首页语义已在 2026-05-31 调整，以后续章节为准：

- 首页在该阶段曾作为综合首页；当前已调整为直播推荐流。
- 底部标签栏为 `首页 / 分类 / 发现 / 关注 / 我的`，除首页外其余入口当前仅预留占位。
- 搜索页支持同时搜索拍品、直播间、商家，结果按 `拍品 / 直播 / 商家` 三个 Tab 展示。
- 拍品搜索支持排序、拍卖状态、商品类别筛选；直播搜索支持排序、直播状态筛选；商家详情页支持商家拍品筛选。
- 点击进行中拍品会直接进入对应直播间，并通过 `lotId` 查询参数打开商品弹窗；非进行中拍品进入 `/product/:lotId` 商品详情页。
- 已结束直播间展示本场成交详情；待开播直播间展示下场待拍商品；正在直播间保留原视频模拟、数字人视频源、商品弹窗、报名、手动出价与支付链路。
- 当前商品/商家图片使用文字占位图组件，但模型保留 `imageUrl`，后续 REST 返回图片后可直接展示。

本轮仍以本地 Demo 数据为首版交互基准，不等待后端搜索/商家/分类接口。

## 2026-05-30 首页、发现与我的页补齐

本轮继续补齐底部标签栏中的高频用户入口：

- 首页分类区改为三行横向滑动布局，并扩展本地 Demo 分类数量，支持点击进入 `/category/:categoryId`。
- 分类详情页复用拍品搜索列表能力，固定 `categoryId`，支持排序和拍卖状态筛选。
- 首页在该阶段曾包含随机拍品区；当前主入口已调整为直播推荐流，不再保留随机拍品区。
- “发现”标签页在该阶段改为竖向直播推荐流，只展示正在直播的直播间，当前可见视频播放，离屏视频暂停。
- “我的”标签页新增个人中心，展示头像、昵称、统计值，以及 `全部 / 待竞价 / 待支付 / 待发货 / 待收货 / 已完成` 六类竞拍/订单记录。
- 头像点击后先展示大图预览；选择本地文件后进入头像编辑弹层，支持拖拽、缩放、圆形裁剪实时预览，确认后以 canvas 输出本地头像。
- 设置页 `/settings` 首版支持修改昵称。

新增前端模型：

```ts
UserProfile
UserAuctionRecord
MyAuctionTabKey
OrderFulfillmentStatus
AvatarCropState
```

新增服务层方法：

```ts
getMyProfile()
updateMyProfile(profile)
listMyAuctionRecords()
```

首版头像和昵称只保存到本地 profile store，不调用真实上传接口。真实后端接入时直接对齐 `UserProfile`、`UserAuctionRecord`、`Order.fulfillmentStatus` 模型，不保留旧字段兼容层。

“我的”页记录分组由前端派生，不再依赖旧的 `waiting / bidding / ended / won` 记录状态：已缴保证金的记录进入 `全部`；未开始或进行中的已缴保证金记录进入 `待竞价`；未支付订单进入 `待支付`；已支付订单按 `fulfillmentStatus=UNSHIPPED / SHIPPED / RECEIVED` 分别进入 `待发货 / 待收货 / 已完成`。竞拍失败、流拍或已退保证金但没有订单履约状态的记录只保留在 `全部`。

## 2026-05-30 导航与头像交互修正

本轮根据验收反馈继续调整用户端交互：

- 底部标签栏移除 `分类`、`关注`，保留 `首页 / 发现 / 我的`。分类能力仍作为首页分类区和 `/category/:categoryId` 详情页存在，不再占用底部主导航。
- 发现页进入直播间时，前端路由增加 `from=discover` 来源参数。直播间返回按钮优先回到来源页；同时新增 `popstate` 监听，浏览器返回也能正确同步到发现页。
- 发现页 Demo 数据补充第二个正在直播的直播间，用于验证竖向滑动切换和多直播推荐流体验。
- 头像裁剪框新增 PC 端滚轮缩放，滚轮上滑放大、下滑缩小，缩放边界与滑杆一致。

已使用根目录 `头像.png` 进行浏览器验收，覆盖本地选图、滑杆缩放、滚轮缩放、拖拽位移、圆形裁剪预览、保存头像，以及发现页滑动切换和从直播间返回发现页。

## 2026-05-31 导航、评论与视频源配置重构

本轮将底部主导航重映射为：

- `首页`：直播推荐流，保留竖向直播间浏览和进入直播间能力。
- `发现`：拍品列表，复用拍品搜索页的排序、状态、类别筛选和拍品点击跳转规则。
- `我的`：个人中心使用六类竞拍/订单状态，待支付记录可直接进入模拟支付页。

旧首页的搜索栏、三行分类和随机拍品区不再作为主入口保留；分类详情、搜索、商家详情、商品详情路由仍按已有能力存在。

直播间 UI 调整：

- 左下角旧状态文案块移除，改为评论列表与评论输入区。
- 评论列表新消息从底部追加，支持滚动；评论输入支持按钮发送和回车发送，空消息不发送。
- 评论开关使用 `Icon/comment.svg` 和 `Icon/close_comment.svg`，关闭后只保留悬浮开关。
- 竞拍状态反馈通过系统评论写入评论流，避免占用固定文案区域。

视频源策略：

- 用户端不再提供视频源切换按钮。
- `LiveRoom.videoSource` 决定直播间渲染实景视频或数字人。
- `recorded` 需要 `videoUrl`；`digitalHuman` 需要 `digitalHuman.idleVideoUrl` 和 `digitalHuman.speakingVideoUrl`，可选 `digitalHuman.ttsWsUrl`。
- 配置缺失时直播间显示视频源配置缺失，不自动回退旧字段。

## 2026-05-31 直播间返回、数字人与评论开关修正

本轮继续修正直播间交互细节：

- 数字人直播只呈现商家配置的视频画面，用户端不显示播报文本、声音、发送播报、停止、重连等控制元素。
- 进入直播间时记录来源路由；退出直播间时返回进入前页面。从首页直播推荐流进入时，退出后回到对应直播间预览位置；从发现拍品列表进入时，退出后回到发现页。
- 直播间进入和返回页面增加轻量过渡动画，支持浏览器 View Transition 时优先使用原生过渡。
- 底部标签栏贴合屏幕底部，移除额外安全区空隙。
- 评论开关图标统一做白色高对比显示，关闭评论后保留醒目的悬浮开关。

## 2026-05-31 移动触控交互修正

本轮基于“移动端触控优先，PC 兼容开发验收操作”的项目记忆，对已有交互做了一轮检查和修正：

- 首页直播推荐流不再依赖浏览器原生滚动和 `scroll-snap`，改为受控全屏轮播，隐藏侧边滚动条。
- 移动端上滑进入下一场直播，下滑回到上一场直播；最后一场继续上滑时，第一场会作为下一场从底部进入，动画结束后静默归位；第一场下滑时，最后一场会作为上一场进入。
- PC 端可使用鼠标左键按住拖拽模拟移动端滑动，也可用鼠标滚轮切换直播间。
- 头像裁剪框支持移动端单指拖拽、双指缩放和双指中心移动；PC 端继续支持鼠标拖拽移动和滚轮缩放。
- 新增回归测试覆盖直播流循环切换、头像触控拖拽、双指缩放和滚轮缩放。

## 2026-05-31 首页直播流循环方向修正

本轮修正首页直播推荐流的循环动画方向：原实现直接在真实索引间取模，最后一场上滑到第一场时会从尾部位置反向跳回顶部。现在滑轨渲染为“首尾克隆 + 原列表”，最后一场继续上滑时先动画到尾部的第一场克隆项，保持与普通上滑一致的方向；动画结束后再无动画归位到真实第一场。第一场下滑到最后一场也使用同一策略。

## 2026-05-31 我的页关注、足迹与订单入口重构

本轮将“我的”页从直接展开竞拍记录列表调整为更符合移动端订单中心的入口结构：

- 顶部统计删除 `提醒`、`收藏`，仅保留可点击的 `关注`、`足迹`。
- 关注按直播间维度实现。直播间右上角按钮支持 `关注 / 已关注` 切换；`/following` 可进入对应直播间或取消关注。
- 足迹只在用户进入完整直播间 `/live/:roomId` 时记录，首页直播推荐流的被动滑动不记录足迹。
- 同一直播间重复进入只保留最近一条足迹并移动到最前，本地最多保存 100 条；`/footprints` 默认展示 10 条，下滑到底或点击加载区后再追加 10 条。
- “我的订单”模块参考 `参考UI/订单列表参考UI.jpg`：`全部` 为单独入口，五个状态 `待竞价 / 待支付 / 待发货 / 待收货 / 已完成` 固定为同一行五列触控入口。
- 完整竞拍/订单列表统一迁移到 `/orders?tab=...`，待支付记录继续提供 `去支付` 操作。

新增本地持久化 store：

```ts
FollowedLiveRoom
LiveRoomFootprint
useLiveActivityStore
```

本轮仍不新增真实后端依赖。关注、取消关注、足迹记录和分页读取均为本地演示能力，后续接入后端时直接对齐 `doc/接口对齐文档.md` 中的接口建议，不保留旧的提醒/收藏入口兼容逻辑。

## 2026-06-01 首页直播流与底部导航视觉修正

本轮修正首页直播推荐流的循环体验和底部导航视觉：

- 直播流保留“首尾克隆 + 原列表”循环策略，但增加过渡锁。动画过程中重复滚轮、拖拽或触屏滑动不会再次触发切换，避免滑轨状态被连续事件打乱。
- 首尾克隆项归位时先关闭 transition 并同步到真实项，再延后一到两帧恢复 transition，确保最后一场继续上滑进入第一场后不会出现第二段“跳回第一场”的可见动画。
- 正式直播间实景视频 `.live-video` 和数字人视频 `.digital-human-video` 改为 `object-fit: contain`，在黑底容器中等比完整显示。首页推荐流预览 `.discover-video` 仍使用 `cover` 保持沉浸式浏览。
- 页面布局新增 `--bottom-tab-height`，`tab-shell`、`tab-content`、首页直播流高度统一基于该变量计算，首页直播预览区域与底部标签栏顶边贴合。
- 底部标签栏改为整块统一背景，按钮自身透明且无独立圆角背景；选中态仅高亮图标和文字颜色。

## 2026-06-01 首页直播间预览交互与 UI 优化

本轮继续细化首页直播推荐流的预览层和触控行为：

- 预览信息层改为贴近短视频直播入口：左下角从上到下展示 `正在直播` 标识、观众数、`@商家名称`、直播间标题和当前拍品信息。
- 当前拍品信息固定在底部信息区内，使用缩略图 + 拍品名 + 当前竞拍价结构；没有当前拍品时仅隐藏拍品卡片，不影响直播状态、商家、标题和进入按钮。
- `进入直播间` 从信息流中移出，放置在画面居中偏下位置，避免和上下滑动手势、底部拍品信息互相干扰。
- 首页直播流从“松手后判断手势”升级为跟手拖动：触摸或鼠标按住移动时，滑轨 transform 实时叠加像素偏移，保证被按住的垂直位置跟随触控点。
- 松手阈值为预览高度的 20%。超过阈值完成上一场或下一场切换；未超过阈值则回弹到当前直播间。PC 滚轮仍保留一格切换，仅作为开发和验收辅助。
- 观众数仅展示 `LiveRoom.watcherCount`。首页预览滑过直播间不计入观众数或足迹；只有进入完整直播间 `/live/:roomId` 后才走足迹记录和实时订阅。
- 正式直播间 `/live/:roomId` 不绑定首页直播流上下滑事件，用户不能在直播间内直接上下切换其他直播间。

## 2026-06-01 首页直播间预览顺滑度与点击入口优化

本轮针对首页预览流继续做移动端触控优化：

- 拖动过程中不再使用 React state 保存每一帧偏移，而是用 ref 保存手势状态，通过 `requestAnimationFrame` 直接写滑轨 `transform`，减少跟手拖动时的重渲染和卡顿。
- React 状态只在释放后更新真实 `activeIndex/trackIndex`；首尾克隆循环、20% 释放阈值和 PC 滚轮切换逻辑保持不变。
- 整个直播间预览区域支持短按进入直播间。判定规则为按下小于 250ms 且移动小于 8px；长按、拖动和切换动画不会触发进入，降低误触。
- 当前拍品信息压缩为单行横向条，缩略图约 35px，商品名位于中间，当前竞拍价在最右侧右对齐，整体高度约 44px。
- `进入直播间` 按钮上移以避开标题和拍品条，按钮背景使用 `rgba(255,255,255,0.05)`，边框使用 `rgba(255,255,255,0.7)`，文字为白色，字体栈优先使用 `"Microsoft YaHei UI Light"`。

## 2026-06-01 语言设置与预览样式修正

设置页新增语言设置项，当前支持 `简体中文` 和 `English`。语言偏好由 `src/store/preferences.ts` 持久化到本地 `aieas-user-preferences`，默认 `zh-CN`；切换后根组件重新创建运行时 translator，页面内已有文案立即随 locale 刷新。紧凑数字和日期格式也改为读取当前 locale，避免 English 界面继续使用中文格式。

首页直播间预览中观众数量胶囊移除边框，保留半透明背景、图标和文字，降低信息层视觉噪声。

## 2026-06-01 首页沉浸式导航与拍品栏对齐

当前标签页为 `首页` 时，底部标签栏使用深色沉浸式背景，未选中标签使用浅色图标和文字，当前 `首页` 标签使用纯白图标和文字。切换到 `发现` 或 `我的` 时，底部标签栏仍使用原浅色背景和红色选中态，避免影响普通内容页的阅读环境。

底部标签栏不再按视口或 H5 外层边框盒独立取宽，而是通过 `--app-shell-border-width` 和 `--app-shell-max-width` 锚定到 H5 内容盒内侧。这样在桌面模拟器、移动端窄屏和 460px 最大宽容器下，标签栏左右边界都与上方直播预览区域无缝衔接。

首页直播间预览底部当前拍品信息栏改为在标题同一左右边距内铺满并居中。该信息栏仍保持单行横向结构：左侧缩略图、中间拍品名、右侧当前竞拍价格，缩略图和整体高度不扩大，避免与进入直播间按钮或底部标签栏重叠。

## 2026-06-01 直播间内部 UI 与预览拍品栏优化

直播间左上角状态区仅保留观众数量，展示样式与首页直播预览的观众数胶囊一致：全透明背景、图标加数字、无边框。直播间内观众数整体透明度为 0.7，在线人数不再作为固定悬浮信息展示，避免左上角信息堆叠。

评论输入区和竞拍清单入口合并到底部同一行。开启评论时底部依次为评论开关、输入框、发送按钮、竞拍清单按钮；关闭评论时保留透明评论开关和竞拍清单按钮。评论开关在开启和关闭状态下保持同一触控尺寸，关闭态不显示背景色、边框或阴影。

直播间当前竞拍拍品小窗只在存在 `RUNNING / EXTENDED` 拍品时显示，并位于竞拍清单按钮上方。当前拍品选择规则为：优先使用 `room.activeAuctionId` 对应且处于竞拍中的拍品，否则取本直播间首个竞拍中拍品；没有竞拍中拍品时不显示小窗。点击小窗进入拍品详情弹层，再进行报名、查看规则或出价。

首页直播预览拍品栏改为“竞拍中优先、即将开拍兜底”：优先展示本直播间 `RUNNING / EXTENDED` 拍品；没有竞拍中拍品时展示下一个 `UPCOMING / READY / WARMING_UP` 拍品；都不存在时隐藏拍品栏。价格左侧新增状态标识，复用 `竞拍中 / 即将开拍` 本地化文案。

## 2026-06-01 直播间快速出价与竞拍小窗优化

本轮将直播间当前竞拍拍品小窗与出价流程调整为更适合移动端单手操作的快速链路：

- 当前竞拍拍品小窗与底部竞拍清单按钮共用 `--live-auction-action-width`，右侧对齐。竞拍清单按钮改为更醒目的红色渐变样式。
- 小窗内容从上到下为拍品缩略图、单行省略拍品名、当前最高出价、最高出价者用户名或 `起拍价`、剩余倒计时和操作按钮。
- 用户未缴纳对应拍品保证金时，小窗按钮显示 `去看看`，点击按钮或小窗任意区域均打开拍品详情弹层。
- 用户已缴纳对应拍品保证金时，小窗按钮显示 `快速出价`。点击小窗非按钮区域仍打开拍品详情，点击按钮打开快速出价弹层。
- 拍品详情弹层和快速出价弹层均使用遮罩淡入与底部滑入动画，点击遮罩可关闭。
- 直播间弹层层级高于直播间头部、评论区、当前竞拍拍品小窗和竞拍清单按钮；存在打开中或已打开弹层时隐藏当前竞拍拍品小窗，关闭动画开始后小窗先恢复到 DOM，由遮罩自然遮挡并在动画结束后露出。
- 快速出价弹层替换旧手动输入出价 UI，展示竞拍倒计时、商品简介、当前价及出价者、我的出价、出价选择器、确定出价按钮和封顶价说明。
- 出价选择器默认取当前价的下一档，单次最多累计三次加价；当封顶价与当前价差小于加价幅度时，下一档直接落到封顶价。
- 快速出价校验包含封顶价、当前价失效、最小出价间隔和竞拍结束状态。Demo 默认最小出价间隔为 1000ms，真实接口可通过 `ruleSnapshot.minBidIntervalMs` 返回。
- 提交出价后弹层不立即关闭，而是等待 `bid.ack`、`bid.accepted` 或 `bid.rejected` 反馈。失败时在弹层内展示明确原因，例如 `手慢了，当前价已更新，请重新加价`。
- 用户停留在快速出价弹层时，如果其他人出价导致当前价更新，已选价格不再高于当前价时确定按钮会禁用；点击加号会基于最新当前价重新加价。
- 用户停留在快速出价弹层时，如果当前商品竞拍结束，弹层倒计时文案变为 `当前商品竞拍已结束`，按钮变为 `该商品拍卖已结束，5s后自动返回直播间` 并倒计时关闭。

`auction.closed` 现在只更新直播间内商品状态，不再自动跳转到成交结果页。成交结果仍可通过结果页路由和后续订单链路进入，避免商品结束事件打断直播间浏览和快速出价关闭动画。

## 2026-06-02 直播间结束态、竞拍小窗收回动画与弹层动效修正

本轮继续修正直播间竞拍结束后的视觉反馈和弹层生命周期：

- 快速出价弹层首次检测到当前拍品不再处于 `RUNNING / EXTENDED` 后记录关闭时间，并以 `closedAtMs + 5000ms` 派生自动返回倒计时。按钮文案稳定显示 `5s -> 0s`，到 `0s` 后触发关闭动画，不再依赖弹层内部 `setInterval`。
- 当前竞拍拍品小窗仅在直播间页面内可见、且没有打开快速出价/拍品详情/竞拍清单弹层时，才会在收到 `auction.closed` 后进入结束态。结束态保留约 5 秒，按钮变为禁用的 `竞拍结束`。
- 结束态小窗到点后进入 `is-leaving` 阶段，播放约 380ms 的竖直下收动画，视觉上收回到同宽右对齐的 `竞拍清单` 按钮方向，动画结束后卸载。
- 竞拍清单、拍品详情和快速出价弹层统一使用同一套 `AnimatedSheetFrame`：打开时遮罩淡入、面板自底部滑入；关闭时遮罩淡出、面板下滑退出，并在动画完成后才卸载。
- 弹层动画按内容高度分档：竞拍清单约 340ms，拍品详情约 400ms，快速出价约 460ms；同时支持 `prefers-reduced-motion: reduce`，便于系统降低动态效果时快速过渡。

## 2026-06-02 底部标签栏复用与宽度稳定性修正

本轮修正“发现”页底部标签栏没有占满一行、与“我的”页视觉宽度不一致的问题。底部导航现在由 `BottomTabBar` 统一渲染，`MainTabShell` 只负责传入当前标签和切换回调，避免在不同页面重复维护导航 DOM 或分散样式。

布局上新增 `.bottom-tab-frame` 作为固定定位外层，统一使用 `--app-shell-max-width` 和 `--app-shell-border-width` 锚定到 H5 内容盒内侧；`.bottom-tabs` 改为普通 100% 宽度三列网格，只负责背景、图标、文字和触控区域。这样“首页 / 发现 / 我的”三页底栏都继承同一条宽度边界，后续不应再为单个标签页单独覆写底栏宽度。

现有视觉规则保持不变：当前标签为“首页”时使用深色沉浸式底栏；当前标签不是“首页”时使用浅色底栏；选中态只高亮图标和文字，不增加独立按钮背景。

## 2026-06-02 直播间实时排行榜与弹层并行动效

直播间右侧在关注按钮下方新增吸附侧边的实时排行榜，默认展开。排行榜仅在当前直播间存在 `RUNNING / EXTENDED` 拍品时渲染；没有竞拍中拍品时不显示排行榜面板、展开按钮或收起按钮。排行榜背景使用约 `rgba(0,0,0,0.2)` 的高透明度黑底，不显示“当前出价前8名”等说明文本，以减少对直播画面的干扰。展开/收起按钮改为竖排文字、圆角矩形透明描边样式，文字在上、箭头在下；展开时贴在排行榜左侧，收起时贴在屏幕右侧。

排行榜顶部显示居中的 `实时排行榜`，中间固定 8 个名次槽位。出价人数不足 8 人时，剩余槽位不显示用户名和头像，出价使用 `-` 占位。底部通过虚线分隔固定展示当前用户信息；当前用户未出价时，名次和出价均为 `-`。前三名使用带数字的金银铜牌，4 名及以后使用无背景、无边框的纯数字。第一名用户名使用金色，第一名出价比其他出价大约 20%，所有有效出价金额使用红色，并使用统一固定价格列居中对齐。排行榜面板高度按标题、8 个槽位、分隔线和当前用户栏贴合内容，避免当前用户栏下方出现多余空白。

排行榜动画由最近一次 `bid.accepted` 与随后的 `ranking.updated.items` 共同驱动。仅当位次发生变化或前 8 名发生进出时播放横条移动动画：前 8 名内用户升至第一时，横条从原位次浮起并移动到第一名；8 名外普通用户升至第一时，横条从虚线处分隔区域出现，同时第 8 名复用普通排行榜行样式，不作为放大的 ghost 渲染，按与 1-7 名相同的速度下移到虚拟第 9 名位置，并由前 8 名裁剪区隐藏虚线以下部分；当前用户在 8 名外或未出价时升至第一，则从底部当前用户栏复制横条起跳。当前用户固定信息栏始终固定在虚线下方，不参与 `is-exiting` 或下移动画；自出价动画第一阶段保留旧位次和旧价格，第二阶段再同步更新为新位次和新价格。其他用户动画约 0.5s，当前用户动画约 1s，当前用户横条放大幅度更强。若当前第一名再次出价但位次不变，不播放横条移动动画，只在出价数值处播放轻量滚动反馈。若动画未结束又收到新的出价，前端会直接结束旧动画并以最新排名快照开始新动画。排行榜价格列需要容纳第一名放大后的完整金额，避免小数位被截断。

直播间弹层生命周期从互斥 boolean 改为 `LiveSheetInstance[]` 实例状态机。每个弹层实例包含 `type / lotId / phase(opening|open|closing) / variant`，关闭时先进入 `closing` 并等待对应动画时长后卸载；打开时先挂载实例再播放进入动画。因此在拍品详情页点击 `去出价` 时，详情页关闭动画与快速出价弹层打开动画可以并行执行，避免中间空白或闪烁。

当前竞拍拍品小窗的显示时机也随状态机调整：当只有关闭中的弹层存在时，小窗会在关闭动画开始前恢复渲染，由弹层遮罩和面板遮挡；关闭动画结束后自然可见。若快速出价、拍品详情或竞拍清单仍处于 opening/open 状态，小窗仍保持隐藏，避免与窄屏底部操作区冲突。

## 2026-06-02 当前竞拍拍品小窗生命周期修正

直播间右下角当前竞拍拍品小窗现在由 `FloatingAuctionCardState` 统一管理，不再由“当前 activeLot 直接渲染”和“结束态 closing card”两套逻辑并行控制。状态包含：

- `mode`：`running / ended`。
- `phase`：`entering / visible / holding / leaving`。
- `auctionId / lot / state / ranking / enrolled` 快照，用于在动画期间保持展示稳定。

进入直播间时，如果当前没有 `RUNNING / EXTENDED` 拍品，不显示小窗；如果已有竞拍中拍品，则直接显示对应小窗。用户在直播间停留期间收到 `auction.started` 后，前端会按“同一直播间最多一个竞拍中拍品”的规则，将同房间其他 `RUNNING / EXTENDED` 本地状态收束为结束态，再把新拍品小窗以 `is-entering` 从底部 `竞拍清单` 按钮处竖直上弹。

收到 `auction.closed` 时，只有当前小窗实际可见且没有打开中的快速出价、拍品详情或竞拍清单弹层，才进入结束态。结束态倒计时区域显示 `END`，操作按钮显示禁用的 `竞拍结束`，保留约 5 秒后进入 `is-leaving` 并竖直下收入 `竞拍清单` 按钮。若 5 秒保留期间又收到新拍品 `auction.started`，旧小窗立即进入下收动画，动画结束后新小窗再上弹，避免两个小窗同时占据右下角。

小窗右上角新增无背景、无边框的 `×` 关闭按钮，aria 文案为 `auction.hideCurrentLot`。用户点击后只临时隐藏当前 `auctionId` 的小窗，后续同一拍品的倒计时或价格刷新不会重新唤起；下一件拍品开拍时会自动解除隐藏并正常弹出。

已登录直播页的竞拍状态 `placeholderData` 改为 memo 化，避免 `stateFromLot(activeLot)` 每次 render 都生成新对象并触发 React Query observer 与小窗派生 effect 的重复更新。

## 2026-06-02 App 路由与模块拆分重构

本轮引入 `react-router-dom` 替代 `App.tsx` 内手写路由逻辑。`App.tsx` 现在只负责挂载 `BrowserRouter` 和应用路由入口；路由表、登录守卫、URL 参数读取和页面导航编排迁移到 `src/router/AppRouter.tsx`；主标签页与底部导航抽离到 `src/layout/MainTabShell.tsx`，避免各标签页重复维护导航 DOM 和样式。

当前业务 URL 保持不变：

- `/login`
- `/`
- `/discover`
- `/me`
- `/category/:categoryId`
- `/search?q=&tab=`
- `/merchant/:merchantId`
- `/product/:lotId`
- `/live/:roomId?lotId=&from=`
- `/result/:auctionId`
- `/pay/:orderId`
- `/settings`
- `/orders?tab=all|pendingBid|pendingPay|pendingShipment|pendingReceipt|completed`
- `/following`
- `/footprints`
- `/history`

登录守卫由 React Router 实现：未登录访问业务深链时跳转 `/login`，并通过 location state 记录原目标；登录成功后使用 `replace` 回到原深链，避免登录页残留在返回栈中。已登录访问 `/login` 时会回到记录的目标页或默认首页。

直播间返回逻辑不再依赖自维护 Route union。进入直播间时，前端通过 location state 保存 `returnTo`，并继续在 URL 中保留 `from` 和 `focusRoomId` 兜底：从首页直播流进入时返回对应直播预览，从发现页或其他业务页进入时返回原页面；刷新直播间后也能基于查询参数回到稳定兜底页面。

测试侧新增 `renderWithRouter`，用于按初始路径渲染应用并验证深链、登录守卫和页面跳转。现有直播间、关注、足迹、订单、设置、快速出价和排行榜链路均继续通过 App 级回归测试覆盖。

## 2026-06-03 直播预览进度承接与竞拍清单半屏列表

首页直播推荐流进入正式直播间前，前端会读取当前激活预览视频元素的 `currentTime`，并通过 React Router location state 传入 `/live/:roomId`。该状态包含 `roomId`、`sourceUrl`、`currentTime` 和 `capturedAtMs`，只在房间和视频源 URL 匹配、时间值有效且快照未过期时应用。非首页入口、刷新直播间、跨视频源或无法读取时间时保持原有从头播放行为。

正式直播间的 recorded 视频在元素挂载、`loadedmetadata` 和 `canplay` 阶段都会尝试应用初始进度并继续播放，覆盖真实浏览器 metadata 晚于 React 挂载到达的情况。数字人直播仅对 idle 视频源做同源进度承接；speaking 视频仍由数字人状态控制，不做跨文件时间映射。

直播间 `竞拍清单` 弹页改为固定半屏面板：`height/max-height` 为 `min(50dvh, 420px)`，标题栏固定在面板顶部，商品列表区域占用剩余高度并 `overflow-y: auto`。展示顺序以原始拍品清单为基础，若存在与 `activeAuctionId` 匹配且状态为 `RUNNING / EXTENDED` 的拍品，则该拍品自动置顶；列表项仍显示其原始业务序号，例如 `#2`，不会因为置顶而重排序号。点击列表项继续沿用已有拍品详情、报名和快速出价链路。
